import sys;_p=r'\\wsl.localhost\Ubuntu-24.04\home\devton_dack_lts\Multi_Filtering_Outliner';sys.path[:]=[_p]+[x for x in sys.path if x!=_p];exec("[sys.modules.pop(n,None) for n in sorted([n for n in list(sys.modules.keys()) if n=='tools' or n.startswith('tools.') or n=='ui' or n.startswith('ui.')],key=lambda x:x.count('.'),reverse=True)];from ui.multi_filtering_outliner_ui import create_multi_filtering_outliner_tab;globals().update({'multi_filtering_outliner_window':create_multi_filtering_outliner_tab()})")
