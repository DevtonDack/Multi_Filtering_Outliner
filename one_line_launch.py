import sys;sys.path.insert(0,r'\\wsl.localhost\Ubuntu-24.04\home\devton_dack_lts\Multi_Filtering_Outliner')if r'\\wsl.localhost\Ubuntu-24.04\home\devton_dack_lts\Multi_Filtering_Outliner'not in sys.path else None;exec("import importlib;[sys.modules.pop(n,None)for n in sorted([n for n in list(sys.modules.keys())if n in['ui','tools']or n.startswith('ui.')or n.startswith('tools.')],key=lambda x:x.count('.'),reverse=True)];from ui.multi_filtering_outliner_ui import MultiFilteringOutlinerWidget;globals().update({'multi_filtering_outliner_window':MultiFilteringOutlinerWidget()});multi_filtering_outliner_window.show()")
